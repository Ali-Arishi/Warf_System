Django>=5.0
gunicorn
Pillow
python-dotenv
boto3
whitenoise
psycopg[binary]
dj-database-url

# deepface removed: pulls TensorFlow (~1.8GB), exceeds the Vercel 500MB limit.
# scipy removed too: it only came in as a deepface dependency; cosine similarity
# is now computed in pure Python inside accounts/services/faceREC/face.py
# To restore face recognition locally or on a host without a size cap:
#   pip install dee